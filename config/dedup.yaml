dedup:
  url_exact: true
  content_hash: true
  vector:
    enabled: true
    threshold: 0.85
    model: ${EMBEDDING_MODEL:-text-embedding-3-small}
    embedding_url: "${EMBEDDING_BASE_URL:-${LLM_BASE_URL:-}}"
    embedding_api_key: "${EMBEDDING_API_KEY:-${LLM_API_KEY:-}}"