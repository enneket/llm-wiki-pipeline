filter:
  mode: keyword              # keyword | llm_judgment

  keyword:
    match_any: true
    tags: [AI, LLM, Rust]

  llm_judgment:
    model: gpt-4o-mini
    sample_rate: 0.1          # 10% 随机采样进 LLM 审核
    min_confidence: 0.7       # keyword 命中但 confidence < 0.7 送审