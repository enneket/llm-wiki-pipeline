filter:
  mode: keyword              # keyword | llm_judgment

  keyword:
    match_any: true
    tags: ${FILTER_TAGS:-AI,LLM,Rust}

  llm_judgment:
    model: gpt-4o-mini
    sample_rate: 0.1
    min_confidence: 0.7